import Testing
@testable import GitOperations

// Worktree tests require actual git repos, so these are minimal unit tests
// for the helper functions. Integration tests will be added when we have
// a test fixture setup.

@Test func placeholder() {
    // WorktreeManager is an actor — tested via integration tests
    #expect(true)
}
